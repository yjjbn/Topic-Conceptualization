You previously classified a set of documents as cases of "populism" or not.
I will give you your original classifications, and your task is to write a qualitative codebook that communicates how you conceptualized and coded the concept "populism."

The input is a JSON object containing:

* `original_classification_instructions`: the instructions used to classify each document;
* `classification_records`: the complete set of documents, assigned labels, the decision basis for each label, and a score on a scale of 0-100 indicating how confident you were in your classification (where 0 is not at all confident, 100 is extremely confident).

Each classification record contains:

* `text`
* `label`
* `decision_basis`
* `confidence`

Based on only these classification records and no other external information, fill in the sections of the codebook below. The codebook should reflect how you originally conceptualized "populism," such that an external coder can identify "populism" in a new set of documents in the same way.

Your codebook should contain the following sections:
1) Definition. Based on decisions with >50% confidence, provide a compact definition for "populism" that, if an independent researcher looks at your definition, they could reasonably make the same coding decisions as you in a new set of documents.

2) Inclusion criteria. Supplement your definition with a set of more specific inclusion criteria. These are criteria that, if the labelled text meets one or some combination of them, the text should be classified as "populism." Make explicit how to apply the criteria, for example, if a specific combination of criteria are needed to classify a text as "populism." 

3) Example text(s) that meets the inclusion criteria and so should be coded as populism.

4) Example text(s) that does not meet the inclusion criteria and so should not be coded as populism.

5) Edge-case rules. Based on cases with less than 50% confidence, write a set of rules that specify how ambiguous or special cases should be coded. These are meant to help the coder in cases where the inclusion criteria might be difficult to apply.

6) Example text(s) that follows the edge-case rules and so should be coded as populism.

Return only a JSON with these fields:

{
  "definition": "Definition of populism",
  "inclusion_criteria": "Inclusion criteria",
  "positive_example": "Example text(s) that meets the inclusion criteria and so should be coded as populism",
  "negative_example": "Example text(s) that does not meet the inclusion criteria and so should not be coded as populism",
  "edge_case_rules": "Rules that specify how ambiguous or special cases should be coded",
  "edge_case_example" : "Example text(s) that follows the edge-case rules and so should be coded as populism"
}

Do not include any text outside the JSON.
